# WELCOME TO JOVIAL-TEXT

`Note: This app is still under development`

To try it, execute the following command in your terminal:
```bash
git clone -b v2 git@github.com:suryaanshrai/jovialText.git && cd jovialText && docker compose up
```